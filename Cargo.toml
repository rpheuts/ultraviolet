[workspace]
resolver = "2"
members = [
    "uv-core",
    "uv-cli",
    "prisms/example/echo",
    "prisms/aws/curl",
    "prisms/aws/burner",
]

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
