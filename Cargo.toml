[workspace]
resolver = "2"
members = [
    "uv-core",
    "uv-cli",
    "uv-service",
    "prisms/example/echo",
    "prisms/aws/curl",
    "prisms/aws/burner",
]

[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
